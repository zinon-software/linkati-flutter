import 'package:flutter/material.dart';

class MediaLinkDetailScreen extends StatelessWidget {
  const MediaLinkDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
